[project]
name = "simple-socks5"
version = "2.0.0"
description = "A Python SOCKS Protocol Version 5 proxy server"
requires-python = ">=3.10"
license = "MIT"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

